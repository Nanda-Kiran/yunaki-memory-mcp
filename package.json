{
  "name": "yunaki-memory-mcp",
  "version": "0.1.0",
  "description": "Git-backed, per-repo memory layer exposed as an MCP server for self-evolving Claude skills.",
  "type": "module",
  "bin": {
    "yunaki-memory-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "start": "node dist/index.js",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
